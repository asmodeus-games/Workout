## Gemini Memories
- The debug APK file located at `app/build/outputs/apk/debug/app-debug.apk` is deliberately tracked and pushed to the repository to allow for easy installation without building from source.
